#DEV
spring.application.name=backend
server.port=8081

# Supabase PostgreSQL via connection pool
spring.datasource.url=jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.ynxyzwfhgqenqyrrhmuf
spring.datasource.password=vUxjYxCfUGp7MFqw

# Hikari connection pool tuning (optional but useful)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=60000
spring.datasource.hikari.max-lifetime=1800000

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
app.jwt.secret=mySecretKey123456789mySecretKey123456789mySecretKey123456789mySecretKey123456789
app.jwt.expiration=86400000

# AI Service Configuration
ai.service.url=http://localhost:8081

# Supabase Storage Configuration
# Note: For file uploads, you need a service role key, not an anonymous key
# The current key appears to be an anonymous key which may not have upload permissions
supabase.url=https://ynxyzwfhgqenqyrrhmuf.supabase.co
supabase.key=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InlueHl6d2ZoZ3FlbnF5cnJobXVmIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTI1MDA3MzksImV4cCI6MjA2ODA3NjczOX0.LC-KCLLgEx_dnhWoGqx7_OUoXvfh5a9YobNIZcDZ6dE
supabase.bucket=utils

spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# PROD
# spring.application.name=${SPRING_APPLICATION_NAME}
# server.port=${SERVER_PORT}

# spring.datasource.url=${SPRING_DATASOURCE_URL}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# spring.datasource.hikari.maximum-pool-size=${SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE}
# spring.datasource.hikari.minimum-idle=${SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE}
# spring.datasource.hikari.idle-timeout=${SPRING_DATASOURCE_HIKARI_IDLE_TIMEOUT}
# spring.datasource.hikari.max-lifetime=${SPRING_DATASOURCE_HIKARI_MAX_LIFETIME}

# spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO}
# spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL}
# spring.jpa.properties.hibernate.dialect=${SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT}
# spring.jpa.properties.hibernate.format_sql=${SPRING_JPA_PROPERTIES_HIBERNATE_FORMAT_SQL}

# app.jwt.secret=${APP_JWT_SECRET}
# app.jwt.expiration=${APP_JWT_EXPIRATION}

# ai.service.url=${AI_SERVICE_URL}
